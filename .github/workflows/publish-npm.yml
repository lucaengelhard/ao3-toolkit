name: "🚀 publish npm"

on:
  push:
    branches:
      - main

jobs:
  release:
    name: 🚀 publish-npm
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 publish
        run: npm publish --access private
        env:
          NODE_AUTH_TOKEN: ${{secrets.NPM_AUTH_TOKEN}}
